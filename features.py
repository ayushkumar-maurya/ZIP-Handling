from tkinter import *


def add_feature(parent):
	Label(parent, text='Select File to Add:').grid(row=0, column=0)


def merge_feature(parent):
	Label(parent, text='Merge two zips together:').grid(row=0, column=0)
